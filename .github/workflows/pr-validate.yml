# Author: Moksh Sharma / DevOps Architecture
# Description: Validates metadata changes on Pull Requests targeting QA, UAT, or Main.

name: Validate Pull Request

on:
  pull_request:
    branches: [ qa, uat, main ]
    types: [ opened, synchronize, reopened, edited ]

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: validate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Delta (${{ github.base_ref }})
    runs-on: ubuntu-22.04
    environment: ms${{ github.base_ref }}
    steps:
      -
        name: Checkout Code
        uses: actions/checkout@v4.2.2
        with:
          fetch-depth: 0

      -
        name: Setup Node.js
        uses: actions/setup-node@v4.1.0
        with:
          node-version: ${{ vars.NODE_VERSION }}

      -
        name: Install Salesforce CLI & SDR
        run: |
          npm install -g "@salesforce/cli@${{ vars.SF_CLI_VERSION }}"
          npm install --no-save "@salesforce/source-deploy-retrieve@${{ vars.SDR_VERSION }}"

      -
        name: Authenticate Target Org
        env:
          SF_JWT_KEY: ${{ secrets.SF_JWT_KEY || secrets.SERVER_KEY }}
          SF_CLIENT_ID: ${{ secrets.SF_CLIENT_ID }}
          SF_USERNAME: ${{ vars.SF_USERNAME }}
          SF_INSTANCE_URL: ${{ vars.SF_INSTANCE_URL }}
        run: |
          set -e
          [ -n "$SF_JWT_KEY" ]      || { echo "::error::Missing SF_JWT_KEY"; exit 1; }
          [ -n "$SF_CLIENT_ID" ]    || { echo "::error::Missing SF_CLIENT_ID"; exit 1; }
          [ -n "$SF_USERNAME" ]     || { echo "::error::Missing SF_USERNAME"; exit 1; }
          [ -n "$SF_INSTANCE_URL" ] || { echo "::error::Missing SF_INSTANCE_URL"; exit 1; }

          if echo "$SF_JWT_KEY" | grep -q "BEGIN"; then
            printf '%s\n' "$SF_JWT_KEY" > /tmp/server.key
          else
            printf '%s' "$SF_JWT_KEY" | tr -d '\r\n ' | base64 -d > /tmp/server.key
          fi
          chmod 600 /tmp/server.key

          sf org login jwt \
            --client-id "$SF_CLIENT_ID" \
            --jwt-key-file /tmp/server.key \
            --username "$SF_USERNAME" \
            --instance-url "$SF_INSTANCE_URL" \
            --alias TARGET_ORG \
            --set-default

      - name: Calculate Delta Changes
        id: delta
        env:
          PACKAGE_DIR: force-app
          BUILD_REASON: PullRequest
          PR_TARGET_BRANCH: ${{ github.base_ref }}
          SF_API_VERSION: ${{ vars.SF_API_VERSION }}
          FORCE_FULL: ${{ vars.FORCE_FULL || 'false' }}
        run: node .github/generate-delta.js

      -
        name: Validate Deployment
        if: steps.delta.outputs.SF_HAS_CHANGES == 'true'
        run: |
          sf project deploy validate \
            --manifest manifest/package.xml \
            --target-org TARGET_ORG \
            ${{ steps.delta.outputs.SF_TEST_ARGS }} \
            --wait 75

      -
        name: Upload Delta Manifest
        if: steps.delta.outputs.SF_HAS_CHANGES == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: sf-manifest-${{ github.base_ref }}-${{ github.run_id }}
          path: manifest/package.xml
          if-no-files-found: error
          retention-days: 14
